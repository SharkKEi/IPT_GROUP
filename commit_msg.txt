fix: post-merge fixes, profile page, modals, and UX improvements

fix(profile): birthday, department, and specialty now save and display correctly
- Fixed MeAPIView to use request.POST instead of request.data for FormData
- Added birthday, department, specialty to user_payload in jwt_serializers.py

fix(auth): session now persists across page refresh
- AuthContext useEffect calls refreshUser on mount to restore session
- loading starts as true so app waits before rendering login or dashboard
- refreshUser now includes birthday, department, specialty in user state

fix(modals): backdrop now covers full screen on all pages
- Applied createPortal to Modal component in StudentsPage, SubjectsPage,
  SectionsPage, EnrollmentsPage, AdminUsersPage, EnrollmentSummaryPage
- Fixed ConfirmDropModal in EnrollmentsPage to use createPortal
- Fixed two-panel enroll modal to use createPortal correctly

fix(serializers): restored enrollment_count and section_count fields lost
during merge conflict resolution
- StudentSerializer: re-added enrollment_count annotation
- SubjectSerializer: re-added section_count annotation

fix(sections): increased section_code max_length from 10 to 50 characters

fix(role): Dashboard Quick Actions now filtered by role using canManageSchool
- Users with user role only see Summary, not staff/admin actions

fix(notifications): student count changes now trigger bell notification
- Student total id now includes count so additions and removals are detected
- Bell auto-polls every 30 seconds for new data
- Unread state persists across navigation using localStorage
